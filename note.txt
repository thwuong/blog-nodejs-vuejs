- database
	+ user(id,username,password,avatar,email)
	+ post(id,user_id,title,body,likes,id_tag)
	+ tag(id,slug,name)
	+ comment(id,user_id,post_id,parent_id,content)
- feature
	+ login,register,updateProfile
	+ CURD post
	+ CURD comment (not update)
- depend
	+ backend : express,cors,body-parser,mongose,dotenv,nodemon,argon2,jsonwebtoken
	+ vuejs : mui,vue-router,pinia,yup,axios,vee-validate
- steps
	+ install library ---------------
	+ connect db -----------
	+ config route
	+ config models
	+ config controller